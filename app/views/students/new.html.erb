<% content_for :title, "新規生徒登録" %>

<div class="max-w-2xl">
  <%= render "shared/back_link", label: "← 生徒一覧", url: students_path %>

  <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-4 sm:p-8">
    <h1 class="text-xl font-bold text-slate-900 mb-6">新規生徒登録</h1>
    <%= render "form", student: @student %>
  </div>
</div>
