<% content_for :title, "タグの編集" %>

<div class="max-w-2xl">
  <%= render "shared/back_link", label: "← タグ一覧", url: tags_path %>
  <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-4 sm:p-8">
    <h1 class="text-xl font-bold text-slate-900 mb-6">タグの編集</h1>
    <%= render "form", tag: @tag %>
  </div>
</div>
